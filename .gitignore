# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# serwist (service worker build output)
/public/sw.js
/public/sw.js.map
/public/swe-worker-*.js

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.1password.prod.tpl
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated/prisma

deploy.tar.gz
deploy_package/
.env.prod-local

# Zaim browser session (contains authenticated cookies/local storage)
/.zaim/

# レシート画像（利用者の購買履歴そのもの）。デプロイのクリーンアップ対象にも入れない。
/storage/

# claude code (個人/セッション固有の設定)
.claude/settings.local.json

# claude-issue-dispatch.yml が無人実行のたびにcheckoutする（リポジトリ管理外）
/.shared-context/
/.shared-prompts/
