<!-- リリースのたびに自動生成されます。手で編集しないでください（guchi-apps/issue-deck#2391） -->

# v7.2.0

データ管理画面のCSVインポートで、すでに登録済みの日の評価額データを「スキップする」か「CSVの値で上書きする」か選べるようになりました。また、資産（カテゴリ）の削除に失敗した場合は、エラーメッセージが表示されるようになりました。

**使い方**

1. 画面上部メニューから「データ管理」を開く
2. 「一括データ登録（インポート）」カードで、登録先の資産を選択する
3. 「3. 評価額がすでにある日の扱い」で「スキップする（既存の値を残す）」または「CSVの値で上書きする」を選ぶ
4. CSVファイルを選択してインポートを実行する
5. 完了メッセージに表示される件数と、選んだ扱い（既存の値が残った/上書きされた）が意図通りであれば成功

